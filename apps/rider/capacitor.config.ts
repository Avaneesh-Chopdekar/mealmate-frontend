import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.mealmate.rider",
  appName: "MealMate for Riders",
  webDir: "dist",
};

export default config;
