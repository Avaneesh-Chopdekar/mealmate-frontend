import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.mealmate.restaurant",
  appName: "MealMate for Restaurants",
  webDir: "dist",
};

export default config;
